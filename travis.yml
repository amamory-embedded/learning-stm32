language: c
dist: trusty
sudo: required

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y gcc-arm-none-eabi

compiler:
  - arm-none-eabi-gcc

env:
  - echo $(PWD)
  - LEARNING_STM32=$(PWD)

script:
  - echo $(PWD)
  - make

notifications:
  email: true